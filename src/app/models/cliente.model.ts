export interface Cliente {

  //atributos ("?" indica que son opcionales los atributos)
  id ? : string;
  nombre ? : string;
  apellido ? : string;
  email ? : string;
  saldo ? : number;

  //constructor

  //métodos

}
